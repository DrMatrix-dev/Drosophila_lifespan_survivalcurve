# ==================== Python 版本 ====================
python>=3.11,<3.12  # 必须 Python 3.11.x

# ==================== 核心关键库 ====================
# 🔥 数据处理核心三件套
pandas==2.3.3        # 数据分析核心
numpy==2.4.2         # 数值计算基础

# 🔥 Excel 文件处理 (关键依赖!)
openpyxl==3.1.5      # 读写 Excel 文件 (.xlsx)

# ==================== 科学计算 ====================
scipy==1.17.0        # 科学计算算法

# ==================== 可视化 ====================
matplotlib==3.10.8   # 图表绘制
seaborn==0.13.2      # 统计图表美化
contourpy==1.3.3     # matplotlib 依赖
cycler==0.12.1       # matplotlib 依赖
fonttools==4.61.1    # 字体处理
kiwisolver==1.4.9    # 布局引擎
pillow==12.1.0       # 图像处理
pyparsing==3.3.2     # matplotlib 依赖

# ==================== 统计分析 ====================
lifelines==0.30.1    # 生存分析，Logrank检验
statsmodels==0.14.6  # 多重比较校正
autograd==1.8.0      # statsmodels 依赖
autograd-gamma==0.5.0 # statsmodels 依赖
formulaic==1.2.1     # statsmodels 依赖
interface-meta==1.3.0 # statsmodels 依赖
patsy==1.0.2         # statsmodels 依赖

# ==================== 工具库 ====================
python-dateutil==2.9.0.post0  # 日期处理
pytz==2025.2          # 时区处理
tzdata==2025.3        # 时区数据
six==1.17.0           # Python 2/3 兼容
packaging==25.0       # 包元数据
typing_extensions==4.15.0 # 类型提示
narwhals==2.16.0      # pandas 兼容层
wrapt==2.1.1          # 装饰器工具
et_xmlfile==2.0.0     # openpyxl 的 XML 处理依赖
